<div class="groups">
  <% link = '../thread/show/'+group.id.to_s %>
  <%= link_to link do %>
    <div style="border: solid 1px #333333;">
      <div>title:<%= group.title %></div>
      <%= group.user.name %>：<% group.posts.find_by(id: 1) %>
      <%= group.posts.first.content %>
    </div>
  <% end %>
</div>
